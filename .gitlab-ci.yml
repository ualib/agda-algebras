image: ruby:2.6

pages:
  stage: build
  script:
    - cp html/UALib.md index.md
    - mv html/*.html html/*.md .
    - bundle install --path vendor
    - bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
